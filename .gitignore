.vscode/**
node_modules
package-lock.json
.idea

# yarn / eslint
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.eslintcache
.DS_Store
.vercel

**/.env*
!**/.env.example

# local tooling / generated state (keep local, do not commit)
# .claude/* rather than .claude/ so the shared skills below can be re-included
.claude/*
!.claude/skills/
.gitnexus/

# stark-smoke-test runtime artifacts
.smoke-test-state.json
.smoke-test-logs/
# personal workflow artifact; this is a public template repo
docs/superpowers/
